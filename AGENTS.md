# Repository Instructions

- After any code change that affects the app, run `pnpm dist:win` before finishing so `dist\win-unpacked\PawPal.exe` is refreshed for local testing.
- Verify the updated `dist\win-unpacked\PawPal.exe` timestamp after packaging. If packaging fails, report the failure and the last successful build state.
